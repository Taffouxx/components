import { ComponentStory, ComponentMeta } from "@storybook/react";
declare const _default: ComponentMeta<(({ user }: import("./UserStatus").Props) => JSX.Element) & {
    displayName: string;
}>;
export default _default;
export declare const Default: ComponentStory<(({ user }: import("./UserStatus").Props) => JSX.Element) & {
    displayName: string;
}>;
